interval:
    every: 1 # Every day: 1, every two days: 2, etc
    time: "02:30" # Run at 2:30 in the morning

notification:
    # Notifications organized in groups
    default:
      discord:
        url: "https:// ...." # Discord webhook
        onUpdate: "<@UserID>" # Custom message
      mail:
        host: "smtp.gmail.com"
        port: 587
        user: "noreply@example.net"
        passwd: "*******"
        from_mail: "noreply@example.net"
        to_mail: ["admin@example.net", "admin2@example.net"]

    test:
      discord:
          url: "https:// ...." # Discord webhook
          onUpdate: "<@UserID>" # Custom message
